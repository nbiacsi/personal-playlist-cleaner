[project]
name = "python-playlist-cleaner"
version = "1.1.0"
description = "Removes songs from Hype playlist that aren't liked"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "spotipy>=2.26.0",
]
